[project]
name = "cdpo"
version = "0.0.1"
authors = [
    {name = "Cory Lorenz", email = "clorenz@gmail.com"},
]

dependencies = [
    "datasets",
    "transformers",
    "torch"
]

[project.optional-dependencies]
tests = [
  "pytest",
  "pytest-cov"
]
dev = [
  "matplotlib",
  "numpy"
]